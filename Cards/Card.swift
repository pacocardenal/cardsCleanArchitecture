import Foundation

public class Card {
    
    let code: String
    let suit: String
    let image: String
    
    init(code: String, suit: String, image: String) {
        self.code = code
        self.suit = suit
        self.image = image
    }
    
}
